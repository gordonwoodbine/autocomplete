import { createTheme } from '@material-ui/core';

const defaultTheme = createTheme({
  palette: {
    primary: {
      main: '#fdb700',
      contrastText: '#fff',
    },
  },
});

export default defaultTheme;
